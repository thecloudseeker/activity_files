name: activity_files
description: >
  A pure Dart toolkit for parsing, editing, and encoding workout activity files
  such as GPX, TCX, and FIT.
version: 0.0.1
homepage:

environment:
  sdk: ^3.3.0

dependencies:
  args: ^2.4.2
  collection: ^1.18.0
  xml: ^6.5.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
