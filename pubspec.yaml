name: activity_files
description: >
  A pure Dart toolkit for loading, editing, validating, and exporting workout activity files—covering GPX, TCX, and FIT — with diagnostics, normalization, and CLI tooling.
version: 0.3.0
repository: https://github.com/thecloudseeker/activity_files
issue_tracker: https://github.com/thecloudseeker/activity_files/issues
topics:
  - gpx
  - tcx
  - health
  - fitness
  - workout

environment:
  sdk: ^3.9.0

dependencies:
  async: ^2.11.0
  args: ^2.7.0
  collection: ^1.19.1
  xml: ^6.6.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
